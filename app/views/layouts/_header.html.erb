<nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%=
            link_to 'Users',
                    users_path,
                    class: "nav-link  #{'active' if current_page?(users_path)}"
          %>
        </li>
       

        <li class="nav-item">
          <%=
            link_to 'Tenants',
                    tenants_path,
                    class:
                      "nav-link  #{'active' if current_page?(tenants_path)}"
          %>
        </li>

         <li class="nav-item">
          <%=
            link_to 'My Tenants',
                    my_tenants_path,
                    class:
                      "nav-link  #{'active' if current_page?(my_tenants_path)}"
          %>
        </li>
        <li class="nav-item">
          <%=
            link_to 'New Tenant',
                    new_tenant_path,
                    class:
                      "nav-link  #{'active' if current_page?(new_tenant_path)}"
          %>
        </li>
        <li class="nav-item">
          <%=
            link_to 'Members',
                    members_path,
                    class:
                      "nav-link  #{'active' if current_page?(members_path)}"
          %>
        </li>
        
      </ul>
      <ul class="navbar-nav mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a
              class="nav-link dropdown-toggle"
              href="#"
              role="button"
              data-bs-toggle="dropdown"
              aria-expanded="false"
            >
              <%= current_user.email %>
            </a>

            <ul class="dropdown-menu">
              <li>
                <%=
                  link_to 'Account',
                          edit_user_registration_path,
                          class: 'dropdown-item'
                %>
              </li>
              <li>
                <%=
                  link_to 'Logout',
                          destroy_user_session_path,
                          method: :delete,
                          class: 'dropdown-item'
                %>
              </li>
            </ul>
          </li>
        <% else %>
          <%=
            link_to 'Sign Up',
                    new_user_registration_path,
                    class: 'btn btn-outline-success me-md-2'
          %>
          <%=
            link_to 'Login',
                    new_user_session_path,
                    class: 'btn btn-outline-danger'
          %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
